query getTranslationsFromBranch($branchId: Int) {
    branches(where: {id: {_eq: $branchId}}) {
        terms {
            id
            key
            translations {
                id
                translation_value
                project_language {
                    language_id
                }
            }
        }
    }
}
