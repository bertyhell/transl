import React from 'react';

// import { useTypedQuery } from '../generated/zeus/reactQuery';
// import { KeyValueEditor } from '../components/KeyValueEditor/KeyValueEditor';
// import { QUERY_KEYS } from '../model/query-keys';

export function TranslationEditor() {
  // const translations = useTypedQuery(QUERY_KEYS.getTranslations, {
  //   translations: {
  //
  //   },
  // })

  return (
    <>
      key value editor: {}
      {/*<KeyValueEditor data={testTranslations} initialData={testTranslations} onChange={() => {}} />*/}
    </>
  );
}
