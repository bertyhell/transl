mutation addBranch($project_id: Int, $value: String, $from_branch_id: Int) {
    insert_translation_events(objects: {project_id: $project_id, value: $value, branch_id: $from_branch_id, operation: "add_branch"}) {
        returning {
            uuid
        }
    }
}
