query getProjects($userUuid: uuid) {
    projects(where: {user_links: {user: {uuid: {_eq: $userUuid}}}}) {
        name
        uuid
        language_links {
            uuid
            language {
                iso_code
                uuid
            }
        }
        company {
            name
            uuid
        }
    }
}
